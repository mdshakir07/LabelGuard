# SQLAlchemy models package. Full schema (PRD §27) lands in A1:
# users, inspections, products, inspection_images, ocr_blocks,
# extracted_fields, rules, assessments, findings, measurements,
# reports, audit_logs.